spring:
  application:
    name: EventClean

  datasource:
    url: jdbc:postgresql://localhost:5431/event-clean_db
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

    jpa:
      show-sql: true

    flyway:
      enabled: true
